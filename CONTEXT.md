# 旅遊行程規劃工具

純瀏覽器的多日旅遊行程規劃工具：規劃每日景點、自動計算時間軸、並可雲端共用行程。本檔僅為領域詞彙表，不含實作細節（實作見 `docs/functional-spec.md` 與 `docs/adr/`）。

## Language

### 核心概念

**行程（Trip）**：
一次完整的旅遊計畫，包含多個「天」、每天的景點與路線，以及設定。
_Avoid_：旅程、計畫

**天（Day）**：
行程中的一天，有出發時間、出發／返回飯店，與一串依序排列的景點。
_Avoid_：日、日程

**景點（Spot）**：
一個要停留的地點，有名稱、類別、停留時間、備註與（選填）座標。
_Avoid_：地點、點位、地標

**路線（Route）**：
兩個相鄰「節點」之間的交通段，記錄交通方式與預計交通時間。
_Avoid_：交通、移動、行程段

**節點（Node）**：
路線的端點，可能是景點、當天的出發飯店或返回飯店。
_Avoid_：端點、站

### 時間

**出發時間（Start Time）**：
一天開始行動的基準時刻；時間軸由此往後推算。

**停留時間（Stay Duration）**：
在一個景點預計停留的分鐘數。

**交通時間（Transit Time）**：
兩節點間移動所需分鐘數。
_Avoid_：路程時間、通勤時間

**時間軸（Timeline）**：
由出發時間、各景點停留時間與各路線交通時間推算出的、每個節點的抵達／離開時刻序列。

**時間槽（Slot）**：
時間軸的計算單位；一個節點或一段路線對應的開始與結束時刻。

**跨午夜（Midnight Crossing）**：
排程延伸超過 24:00 的情況，時間軸須正確顯示。

### 版面

**版面 A（Tab Layout）**：
頁籤式，一次顯示一天，上方頁籤切換。
_Avoid_：分頁模式

**版面 B（Sidebar Layout）**：
側邊欄式，左側列出各天與天氣摘要，右側顯示當天內容。

**版面 C（Scroll Layout）**：
垂直捲動式，所有天同時顯示，適合列印／PDF。

### 景點類別

**類別（Category）**：
景點的分類，決定卡片左側色條。取值：sightseeing（景點）、food（美食）、shopping（購物）、transit（交通）、hotel（住宿）、activity（活動）、other（其他），或未分類。

### 共用

**共用行程（Shared Trip）**：
上傳到雲端（Firestore）的行程，可透過連結分享或匯入。分為公開與私人。
_Avoid_：雲端行程、分享檔

**分享碼（Secret Code）**：
私人共用行程的查詢暗號；只有知道的人能查到該行程。
_Avoid_：密碼、通關碼

**刪除密碼（Delete Password）**：
上傳者用來驗證刪除／覆寫權限的密碼；以 hash 儲存，明碼不落地。

**超級使用者（Superuser）**：
隱藏的 UI 便利模式，非真正權限邊界（見 ADR-0004）。

### 交換與同步（Notion / Markdown / CSV）

**交換格式（Interchange）**：
Trip 與 Notion 互通的載體，分兩個**互相獨立、不可逆**的方向：**匯出**產生 Notion 可 import 的 Markdown（頁面）＋ CSV（資料庫）；**匯入**解析 Notion export 的 Markdown ＋ CSV。行程資料在 CSV（Notion 頁面 md 不含行程列）。不做 MD 的 web↔web 無損來回（JSON 已無損；見 ADR-0007）。
_Avoid_：匯出檔、輸出格式（太籠統）；同步（本專案刻意不做雙向來回）

**Notion 匯出（Notion Export）**：
Notion 匯出的是一個**多檔資料夾**：頂層頁面 md ＋ 每個資料庫一份 CSV ＋ 每列一個子頁面 md。行程資料在 CSV，不在頁面 md。

**取代式匯入（Replace Import）**：
匯入外部資料時，整趟 Trip 以其**新建或整個取代**，不做逐列比對合併、不維護跨系統穩定 ID（見 ADR-0007）。
_Avoid_：同步、合併（本專案刻意不做雙向合併）

**交通段列（Leg Row）**：
在 Notion「行程」表與我們的 CSV 中，代表一段 Route 的那一列——以「移動方式」欄非空判定（備用：Details 為 `"A - B"`）。其餘列為景點列（Spot Row）。

**住宿（Accommodation）**：
一趟 Trip 的一間住宿記錄，含類型、地址、入住／退房日期、城市、花費、付款狀態、訂房連結、圖片。獨立於 Itinerary 的側記錄，不自動注入時間軸。
_Avoid_：飯店（`startHotelName`/`endHotelName` 仍指每天的出發／返回飯店字串，兩者不同）

**航班（Flight）**：
一趟 Trip 的一筆航班記錄，含航空公司、航班號、艙等、出發／抵達機場與時刻、飛行時間、國內／國際。獨立側記錄。

**攻略頁（Guide）**：
附在 Trip 上的自由格式參考頁（如「大阪景點」「梅田攻略」），標題＋內文（Markdown）。對應 Notion「旅遊攻略」資料庫的子頁。
_Avoid_：筆記（`notes` 指景點層級的備註，不同層級）

**待辦（Todo）**：
Trip 層級的檢查清單項目（文字＋完成與否），對應 Notion 頂層頁面的「待辦事項」。
